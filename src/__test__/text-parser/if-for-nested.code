let __ = ''
let __v = ''
with (__ctx__) {
  __ += `before\n`
  if (a === b) {
    for (const a of b) {
      __ += `    do for\n`
      if (c === d) {
        __ += `            c === d\n`
      } else {
        __ += `           else `
        __ += someExpr.toString()
        __ += `\n`
      }
    }
  }
  __ += `text\n`
  for (const a of b) {
    __ += `do for 2\n`
  }
  __ += `after\n`
}
return __