const __ = []
let __v = ''
with (__ctx__) {
  __.push(`before\n`)
  if (a === b) {
    for (const a of b) {
      __.push(`    do for\n`)
      if (c === d) {
        __.push(`            c === d\n`)
      } else {
        __.push(`           else `)
        __.push( someExpr.toString() )
        __.push(`\n`)
      }
    }
  }
  __.push(`text\n`)
  for (const a of b) {
    __.push(`do for 2\n`)
  }
  __.push(`after\n`)
}
return __.join('')