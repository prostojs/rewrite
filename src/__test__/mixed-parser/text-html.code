let __ = ''
let __v = ''
with (__ctx__) {
  __ += `// text part\n`
  if (a === b) {
    __ += `const a = '1'\n`
  }
  for (a ob n) {
    __ += `<div `
    __ += `>\n    `
    __ += `<span `
    __v = c + d
    if (typeof __v === 'boolean') {
      if (__v) __ += `data`
    } else {
      __ += `data="${ (__v || '').replace(/"/g, '&quot;') }"`
    }
    __ += ` />`
    __ += `\n`
    __ += `</div>\n`
  }
  if (b === c) {
    __ += `const b = '2'\n`
  }
}
return __